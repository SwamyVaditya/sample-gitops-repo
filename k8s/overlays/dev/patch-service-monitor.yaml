# k8s/overlays/dev/patch-service-monitor.yaml
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sample-app
  namespace: dev          # <-- forces the namespace to 'dev'

spec:
  namespaceSelector:
    matchNames:
      - dev # Only scrape targets in the 'dev' namespace
